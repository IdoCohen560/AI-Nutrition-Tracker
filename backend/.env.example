SECRET_KEY=replace-with-long-random-string
DATABASE_URL=sqlite:///./nutrilog.db
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Optional: OpenAI for NLP meal parsing (otherwise heuristic parser is used)
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini

# Optional: USDA FoodData Central for nutrition lookup
# USDA_API_KEY=

# NLP HTTP timeout (seconds)
# NLP_TIMEOUT_SECONDS=5
